# PostHog analytics
POSTHOG_PROJECT_TOKEN=
POSTHOG_HOST=https://us.i.posthog.com

# SendGrid email (required on production — SMTP ports are blocked)
# 1. Create a free account at https://sendgrid.com
# 2. Settings → API Keys → Create API Key (Mail Send permission)
# 3. Settings → Sender Authentication → verify FROM_EMAIL as a Single Sender
SENDGRID_API_KEY=
ADMIN_EMAIL=scifables2026@gmail.com
FROM_EMAIL=notifications@scifables.com
FROM_NAME=SciFables
